/** Cookie keys used by the panel's topbar selectors. Plain constants — safe to import from
 * client + server. */
export const ACTIVE_BUILDING_COOKIE = 'evon.activeBuildingId'
export const ACTIVE_PERIOD_COOKIE = 'evon.activePeriod'
