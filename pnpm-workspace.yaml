# pnpm v11+ reads settings from here (no longer from the package.json "pnpm" field).
# Allow these trusted packages to run their native postinstall build scripts.
allowBuilds:
  '@biomejs/biome': true
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
  sharp: true
